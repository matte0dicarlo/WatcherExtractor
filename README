
watcher.py monitors a directory. it does something if a file is created in it.

extractor.py deletes columns from an excel file

watcherextractor removes columns from any excel file created in a directory. it takes as input a dir to monitor and an output dir







# run extractor.py:
python3.8 extractor.py SOMEDIR/input.xls


# run watcherextractor.py:
python3.8 watcherextractor INPUTDIR OUTPUTDIR

